{
  "_README": [
    "Illustrative model registry for Handoff -- the SHAPE of the per-user worker registry.",
    "You normally do NOT hand-write this. Prefer:",
    "    python run/registry.py connect          # find + register a local model server, re-check saved ones",
    "    python run/registry.py add http://localhost:8080   # qualify + register one endpoint",
    "Those write the real registry (with qualification metadata) to the per-user path:",
    "    Windows: %LOCALAPPDATA%/fleet/workers.json   Linux/mac: $XDG_STATE_HOME/fleet/workers.json",
    "That path is per-USER and shared across every clone/project, so a returning user reuses it.",
    "For a PROJECT-LOCAL registry instead, set FLEET_REGISTRY=/path/to/this-project/workers.json",
    "(highest precedence; both the reader and registry.py honour it). Copy this file there and edit.",
    "Delete this _README key in a real file; each other top-level key is one worker NAME.",
    "reasoning_style: none | template_kwargs | budget_field. Leave defaults unless you have measured otherwise."
  ],
  "cluster": {
    "url": "http://localhost:8080",
    "model": "qwen2.5-coder-32b",
    "kind": "openai",
    "backend": null,
    "ctx": 32768,
    "max_inflight": 1,
    "requires_prefill_lock": false,
    "supports_gbnf": false,
    "reasoning_style": "none",
    "source": "registered"
  },
  "spark": {
    "url": "http://localhost:1234",
    "model": "a-small-fast-model-for-the-skeptic",
    "kind": "openai",
    "backend": null,
    "ctx": 8192,
    "max_inflight": 1,
    "requires_prefill_lock": false,
    "supports_gbnf": false,
    "reasoning_style": "none",
    "source": "registered"
  }
}
